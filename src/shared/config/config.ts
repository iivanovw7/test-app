export const config = {
    ui: {
        /** Throttle delay in `ms`. */
        throttle: 1000,
        /** Debounce delay in `ms`. */
        debounce: 300,
    },
    meta: {
        keywords: ["astro", "qwik", "javascript", "typescript"],
        name: "Test app",
        url: "",
    },
};
