---

import RootLayout from "./root-layout.astro";

const { description, slug, tags, title } = Astro.props;

---

<RootLayout description={description} slug={slug} tags={tags} title={title}>
    <slot />
</RootLayout>
